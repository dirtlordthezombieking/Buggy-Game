function Model(glCore,shaderProgram,src)
{
	this.gl=glCore;
	this program=shaderProgram;
	let meshes=src["MESH"];
	meshes.forEach(function(mesh,ind)
	{
		
	});
}
function RawAttribute()
{
	this.parse=function()
	{
	};
}